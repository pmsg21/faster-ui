---
'@pmsg21/faster-ui': patch
---

Initial published release of Faster UI, the PUMA design system foundation. No public components yet — this release establishes the package on npm and the automated release pipeline; `Button`, `Input`, and `Dialog` follow.
